# 华能消费帮扶独立租户需求追踪记录

> 创建时间：2026-03-24  
> 最后更新：2026-03-24

---

## 一、需求概述

:material-help-circle: **背景说明**

华能集团需要将 `goodsType=99` 的消费帮扶商品数据独立存储到专属租户，实现数据隔离和独立管理。

---

## 二、核心配置

### 租户ID对照表

| 环境 | 华能主租户ID | 消费帮扶租户ID |
|------|-------------|---------------|
| 正式环境 | `1706958926295019522` | `2018950843406299138` |
| 测试环境 | `1798558397867671554` | `2019308420509958145` |
| 测试环境(ZN) | `1650033261075759106` | - |

### 数据库映射配置

```sql
-- t_tenant_goods_type_mapping 表
INSERT INTO `operate`.`t_tenant_goods_type_mapping` 
(`id`, `original_tenant_id`, `target_tenant_id`, `goods_type`, `goods_type_name`, `description`, `status`, `create_time`, `update_time`, `create_user`, `update_user`, `is_delete`) 
VALUES (1, '1706958926295019522', '2018950843406299138', '99', '消费帮扶商品', '华能消费帮扶租户映射', 1, '2026-03-24 11:40:38', '2026-03-24 11:40:38', 'SYSTEM', NULL, 0);
```

---

## 三、关键代码文件

### 文件清单

| 模块 | 文件 | 职责 |
|------|------|------|
| `bssc-biz-operate` | `TenantGoodsTypeMappingServiceImpl.java` | 租户映射核心逻辑 |
| `bssc-biz-interchange` | `GoodsController.java` | API商品同步接口 |
| `bssc-biz-entpur` | `GoodsServiceImpl.java` | 商品导入、列表查询 |

### 核心方法

- `isHuaNengTenantId(tenantId)` - 判断是否为华能相关租户
- `getTargetTenantId(originalTenantId, goodsType)` - 获取映射后的目标租户ID

---

## 四、问题排查记录

### 2026-03-24 问题1：中国信保接口JSON解析异常

**错误日志**：
```log
JSONException: Mismatched hr and body at 138 [character 6 line 6]
调用 https://xbsc.sinosure.com.cn/jgjc/serv/interchange-service/api/syncData/syncData2Privatization 接口异常
```

**分析结论**：
- :red_circle: **与本次改动无关**，是中国信保私有化租户的独立问题
- 私有化接口返回HTML页面而非JSON格式
- 原因可能是：接口服务异常、WAF拦截、证书问题等

**处理方式**：
- 不阻塞本次发布
- 后续联系中国信保运维确认接口状态

---

## 五、调试日志关键字

```
租户映射
华能消费帮扶
华能重新设置租户Id
检查租户.*是否为华能租户
goodsType
当前商品类型
```

---

## 六、数据验证SQL

### 1. 验证映射配置

```sql
SELECT * FROM t_tenant_goods_type_mapping 
WHERE original_tenant_id = '1706958926295019522';
```

### 2. 验证商品分布

```sql
SELECT developer_id, goods_type, COUNT(*) as cnt
FROM t_developer_goods 
WHERE developer_id IN ('1706958926295019522', '2018950843406299138')
GROUP BY developer_id, goods_type;
```

### 3. 验证消息生成

```sql
SELECT developer_id, COUNT(*) as msg_count
FROM t_developer_msg_detail 
WHERE developer_id IN ('1706958926295019522', '2018950843406299138')
GROUP BY developer_id;
```

---

## 七、相关文档

- `华能消费帮扶独立租户代码修改说明.md`
- `消息生成链路说明文档.md`
- `价格监测平台 - 项目流程文档.md`

---

## 八、待办/备注

- [ ] 验证正式环境消费帮扶商品数据是否正确隔离
- [ ] 验证消息生成链路是否正常
- [ ] 后续跟进中国信保接口问题

---

*此文件用于追踪华能消费帮扶需求开发过程中的问题和记录，请持续更新。*
