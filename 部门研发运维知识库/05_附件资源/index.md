# 附件资源

本板块用于存放各类附件资源，如图片、文档、压缩包等。

## 资源清单

| 文件 | 说明 | 下载 |
| :--- | :--- | :--- |
| `deploy.sh` | MkDocs 一键提交+部署脚本（git commit + gh-deploy） | [下载](deploy.sh) |

!!! tip "使用方法"
    下载后放入项目根目录，在 Git Bash 中执行：
    ```bash
    chmod +x deploy.sh
    ./deploy.sh "你的提交信息"
    ```
